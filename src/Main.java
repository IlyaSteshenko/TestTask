import managers.LaunchManager;

public class Main {
    public static void main(String[] args) {
        LaunchManager launchManager = new LaunchManager(args);
        launchManager.launch();
    }
}
